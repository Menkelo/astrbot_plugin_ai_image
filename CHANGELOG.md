# Changelog

记录用户可感知的功能与修复。历史从 1.7.6 起按 Git 提交回填；日期采用对应提交日期，同一版本内的多次修补合并说明。未发布的改动单独列出，不改写历史版本的实际行为。

## Unreleased

### 修复

- 修复 1.8.1 的统一补边逻辑给 Gemini/Vertex 正常返回图添加上下或左右白边的问题。指令/预设比例仍传给接口，结果保留原生画幅，只做 EXIF 方向修正与必要的等比放大；OpenAI/GPT 的补边策略保持不变。
- 主备接口切换后按实际成功渠道选择后处理；Gemini 参数兼容降级后的结果也不再被本地补边。

## 1.8.1 - 2026-09-10

### 修复

- 图片比例兜底改为保留完整内容：优先使用指令指定比例，否则读取第一张参考图应用 EXIF 方向后的实际比例；不再就近套用其他比例。
- 移除图生图的“参考图补成正方形 → 生成 → 根据边缘颜色回裁”流程。参考图保持各自画幅，生成结果比例不符时等比适配并补边，不裁剪主体、不非等比拉伸。
- OpenAI、Gemini、Vertex 以及备用接口共用同一个本地目标画幅；接口忽略或拒绝尺寸参数时，本地目标不会随请求降级而丢失。
- GPT Image 2 的任意参考比例转换为符合边长、16 像素对齐、长短边比与总像素约束的请求尺寸；不再发送其不支持的 `input_fidelity` 参数。GPT Image 1 系列使用对应的固定尺寸集合。
- OpenAI 图像接口只在结构化错误明确指出 `size` 或 `input_fidelity` 不兼容时降级一次，并只移除被拒绝的字段；其他 400 不再盲目重发。
- 输入、输出在处理比例前统一应用 EXIF 方向；需要旋转时使用无损 PNG，避免 JPEG 再压缩。
- Gemini/Vertex 的分辨率档位不再套用 GPT Image 2 的尺寸表。补边画布设置像素和边长上限，必要时等比缩小整张结果。
- 分辨率适配与补边一次完成，避免先补边再放大时放大整数像素的舍入误差；保留 21:9 等预设的原生尺寸，避免无意义的再次放大。

### 新增

- `CHANGELOG.md`、[比例问题复盘](docs/IMAGE_RATIO.md)，以及覆盖主体边缘、任意比例、透明度、EXIF、多参考图、参数降级与备用接口的回归测试。
- GPT Image 2.5 Sunburst / Flare 适配：识别原生自定义尺寸能力，1K/2K/4K 按合法尺寸请求，避免退回 `size=auto` 后依靠本地放大。保留用户配置的模型 ID，支持中转站的 2.5 家族别名透传。
- GPT Image 2.5 专属质量配置 `gpt_image_quality`（auto/low/medium/high/xhigh/max）与背景配置 `gpt_image_background`（auto/opaque/transparent），默认均为 auto。其他模型继续使用原有参数。
- 2.5 文生图和图生图统一使用 `output_format=png`，省略旧的 `response_format` 和 `input_fidelity`；透明结果在后续补边、缩放和发送中保留透明度。
- 对结构化的质量、背景或输出格式不兼容错误给出具体提示，保留用户明确选择的选项；尺寸降级只移除被拒绝的尺寸字段。

详见 [GPT Image 2.5 配置与兼容说明](docs/GPT_IMAGE_25.md)。

## [1.8.0] - 2026-09-10

### 新增

- AstrBot Canvas Page「映像」图片管理页，支持亮暗主题与手机布局。
- 自动归档生成图片、提示词和实际成功的模型/提供商信息，数据存放在插件持久化目录。
- 瀑布流浏览、大图预览、搜索筛选、收藏、相册、标签与备注编辑。
- 本地导入与查重、原图下载、批量 ZIP 导出（含元数据）、回收站恢复与永久删除。
- 图库配置、使用说明、31 项后端回归测试，以及使用官方 Bridge 的隔离浏览器验收工具。

## [1.7.9] - 2026-09-09

### 修复

- 扩展正方形补边的回裁判断：除近白区域外，颜色较均匀的边缘（RGB 各通道标准差小于 15）也按空白处理，以应对模型将白色补边填成其他纯色的情况。
- 此时仍使用边缘启发式判断，不能保证主体不被误裁；相关替代方案见 1.8.1 条目。

## [1.7.8] - 2026-09-08

### 修复

- 正方形结果回裁前检查拟删除区域：只有两侧均达到 90% 近白像素时才裁剪；补边被模型画入内容时跳过裁剪，避免直接截掉这些内容。
- 将 HTTP 451 归为内容审核不通过，并加入不切换备用提供商的状态码集合。

## [1.7.7] - 2026-09-07

### 修改

- 取消未指定比例时“按第一张参考图匹配最近的比例枚举，再强制裁剪”的处理。
- 该版本号下进行了多次比例策略调整：先尝试发送对齐后的参考图像素；随后增加回裁到参考图画幅，再改成 `size=auto` 并取消强制回裁；最后改为先把参考图补成白底正方形、按正方形生成后裁回第一张参考图比例。
- 图生图开始传入 `input_fidelity=high`，并尝试在 400 时移除尺寸和保真参数重试。
- 精简 400/422 的用户错误文案。

> 该版本包含多次同版本号提交；最终比例策略对应 `ac1fe88`，并非最初版本号更新时的实现。

## [1.7.6] - 2026-09-07

### 修改

- 用户错误提示简化为单行，移除处理建议与语义关键词分类，主要按 HTTP 状态码归类。
- 同步调整状态提示和部分配置说明。

> 比例处理基线：该版本仍会把参考图映射到最近的固定比例，并在生成后裁剪。此行为在 1.7.7 开始调整，并非 1.7.6 新增。

[1.8.0]: https://github.com/Menkelo/astrbot_plugin_ai_image/compare/a9aec3e...3f0f4eb
[1.7.9]: https://github.com/Menkelo/astrbot_plugin_ai_image/compare/7cd4a77...a9aec3e
[1.7.8]: https://github.com/Menkelo/astrbot_plugin_ai_image/compare/ac1fe88...7cd4a77
[1.7.7]: https://github.com/Menkelo/astrbot_plugin_ai_image/compare/9750bf5...ac1fe88
[1.7.6]: https://github.com/Menkelo/astrbot_plugin_ai_image/compare/74b834d...9750bf5
